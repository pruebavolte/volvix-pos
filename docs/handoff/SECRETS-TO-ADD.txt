# Env vars necesarias (NO commitear con valores reales)
# Setup: Vercel Dashboard → Settings → Environment Variables → Add

## Auth & Core
JWT_SECRET=                    # openssl rand -hex 32
SUPABASE_URL=                  # https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=     # Supabase Project Settings → API
SUPABASE_ANON_KEY=             # Supabase Project Settings → API

## Email
RESEND_API_KEY=                # resend.com → API Keys → Create
RESEND_FROM_EMAIL=             # noreply@tudominio.com (o onboarding@resend.dev)

## SMS / WhatsApp
TWILIO_ACCOUNT_SID=            # console.twilio.com → Account SID
TWILIO_AUTH_TOKEN=             # console.twilio.com → Auth Token (ROTAR si comprometido)
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886  # Sandbox default
TWILIO_SMS_FROM=               # +1XXXXXXXXX (comprar número $1-2/mes)

## Pagos (Stripe)
STRIPE_SECRET_KEY=             # dashboard.stripe.com → Developers → API keys
STRIPE_PUBLISHABLE_KEY=        # pk_test_... o pk_live_...
STRIPE_WEBHOOK_SECRET=         # whsec_... (de webhook endpoint)

## OAuth
GOOGLE_CLIENT_ID=              # console.cloud.google.com → OAuth 2.0
GOOGLE_CLIENT_SECRET=          # console.cloud.google.com → OAuth 2.0

## AI
ANTHROPIC_API_KEY=             # console.anthropic.com → API Keys

## Cron protection
CRON_SECRET=                   # openssl rand -hex 32
REMOTE_SESSION_SECRET=         # openssl rand -hex 32 (para R6a)

## Mobile (cuando buildees)
ANDROID_KEYSTORE_PATH=
ANDROID_KEYSTORE_PASSWORD=
ANDROID_KEY_ALIAS=

## ⚠️ Twilio comprometido en sesión 2026-04-28: ROTAR token urgente
