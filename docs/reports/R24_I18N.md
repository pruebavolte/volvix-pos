# R24 — I18N Audit

**Fecha:** 2026-04-27
**Versión:** verion 340

## 1. Wiring (`volvix-i18n-wiring.js`)

22/34 HTMLs cargan el script. **12 HTMLs sin wiring** (no críticos: paneles internos, demos, backups):

- `BITACORA_LIVE.html`, `MATRIZ_PRUEBAS_LOCAL.html` (+ backup) — herramientas internas
- `volvix-audit-viewer.html`, `volvix-customer-portal-v2.html`, `volvix-gdpr-portal.html`, `volvix-kds.html`, `volvix-kiosk.html`, `volvix-modals-demo.html`, `volvix-onboarding-v2.html`, `volvix-qa-scenarios.html`, `volvix-shop.html`

Todos los HTMLs **públicos/principales** (login, hub, dashboard, owner panel, marketplace, sandbox, etc.) sí cargan i18n.

## 2. Conteo de Keys (R17 baseline = 187)

| es | en | pt | fr | de | it | ja |
|----|----|----|----|----|----|----|
| 187 | 187 | 187 | 187 | 187 | 187 | 187 |

**OK** — paridad total, 7/7 idiomas con 187 keys. No se agregaron keys.

## 3. API `/api/i18n/locales`

Endpoint en `api/index.js:7920`. Retorna las 7 banderas correctas:
ES (MX), EN (US), PT (BR), FR (FR), DE (DE), IT (IT), JA (JP). `default: true` en `es`. **OK.**

## 4. Selector flotante + `Volvix.i18n.setLanguage('en')`

- `window.Volvix.i18n` definido (línea 1703), alias de `window.I18nAPI`.
- `setLanguage('en')` valida lang → guarda en localStorage → `translateAll()` → `updateSelectorButton()` → emite `volvix:langchange`.
- Botón flotante `#volvix-i18n-btn` (top:140px, right:20px) actualiza bandera en vivo. Dropdown lista los 7 idiomas con `LOCALES[code].flag`. **OK.**

## Veredicto: PASS
Sin keys faltantes. Sin cambios necesarios.
