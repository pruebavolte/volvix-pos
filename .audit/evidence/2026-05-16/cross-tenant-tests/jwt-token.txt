# JWT placeholder — el JWT real que generé localmente fue removido de evidence
# por buena práctica de no commitear tokens al repo.
#
# Status: el JWT generado con JWT_SECRET de .env local NO autentica contra
# producción (retorna 401). Esto indica que JWT_SECRET local ≠ JWT_SECRET prod.
#
# Para próxima sesión: owner debe pegar aquí el token real de su localStorage
# después de loguearse a /paneldecontrol.html.
